---
# defaults file for SecureVPS
user_vars: 
  - username: "admin"
    groups: "wheel"
    append_group: yes
    create: present
    delete: no
    keyname: "../files/{{ user_vars.username }}.pub" 

#- name: "Include user variables"
#  include: "manageUsers.yml"